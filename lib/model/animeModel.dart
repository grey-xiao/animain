class AnimeModel{
  final String title;
  final String description;
  final int episodes;

  const AnimeModel({
    required this.title,
    required this.description,
    required this.episodes,
  });
}